import React from 'react';
import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';
import { Target } from 'lucide-react';
export const DirectionProgress: React.FC = () => {
  return <Card className="kamp-card">
      
      
    </Card>;
};